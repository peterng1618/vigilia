{
  "name": "@vigilia/editor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "description": "Desktop authoring experience. Adds interaction overlays and inspectors on top of the shared renderer.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@vigilia/renderer-core": "*",
    "echarts": "6.1.0"
  },
  "//": "The editor foundation (fabric.js via vue-fabric-editor or yft-design, or Fabric 7 direct) is NOT chosen yet — ADR-0001 evaluates both candidates in parallel at Gate 0. Deliberately left undeclared so the decision is made on evidence rather than by whatever got installed first."
}
