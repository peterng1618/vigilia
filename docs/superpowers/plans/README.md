# Implementation plans

Active executable plans live here. Move a completed plan to `archive/`; it is
historical context, not current instruction. `.superpowers/sdd/` is ignored
execution state and points to the plan it executes.
