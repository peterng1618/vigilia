### Task 1: Establish the React and Tailwind build boundary

**Files:**
- Modify: `src/web/packages/editor/package.json`
- Modify: `src/web/package-lock.json` (generated by `npm install`; do not hand-edit)
- Modify: `src/web/packages/editor/vite.config.ts`
- Modify: `src/web/packages/editor/tsconfig.json`
- Modify: `src/web/packages/editor/index.html`
- Modify: `THIRD-PARTY-NOTICES.md`
- Test: `src/web/packages/editor/vite.config.ts` build through the editor script

**Interfaces:**
- Consumes: the existing Vite aliases, relative `base`, and loopback dev server.
- Produces: a JSX-capable editor build with React and Tailwind available only to `@vigilia/editor`.

- [ ] **Step 1: Record package licences before adding dependencies**

Read the npm package metadata for `react`, `react-dom`, `@base-ui/react`, `tailwindcss`, and `@tailwindcss/vite`. Add their exact resolved versions and licence text/source to `THIRD-PARTY-NOTICES.md`.

- [ ] **Step 2: Install the editor-local UI dependencies**

Run: `npm install -w @vigilia/editor react react-dom @base-ui/react tailwindcss @tailwindcss/vite && npm install -D -w @vigilia/editor @types/react @types/react-dom`

Expected: only `src/web/package.json` workspace manifests and generated `src/web/package-lock.json` change; no root-level mockup package files are staged.

- [ ] **Step 3: Add the smallest Vite/TypeScript integration**

Add the official Tailwind Vite plugin beside the existing Vite config and enable `jsx: "react-jsx"` in the editor TypeScript project. Keep aliases, `base: "./"`, `target: "es2022"`, and loopback-only server settings unchanged.

```ts
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  plugins: [tailwindcss()],
  // existing resolve, base, build, and server settings
});
```

- [ ] **Step 4: Replace the static shell container, not the Fabric host contract**

Keep `#stage`, `#properties`, and `#status` available during the bridge transition, but make `#app` the React root. Move component styles out of `index.html` into the shell stylesheet and import Tailwind once:

```css
@import "tailwindcss";
```

- [ ] **Step 5: Prove the build boundary**

Run: `npm run typecheck -w @vigilia/editor && npm run build -w @vigilia/editor`

Expected: both pass and the bundle still resolves the adopted fork at its explicit compiled entry.

- [ ] **Step 6: Commit**

```bash
git add THIRD-PARTY-NOTICES.md src/web/package-lock.json src/web/packages/editor/package.json src/web/packages/editor/tsconfig.json src/web/packages/editor/vite.config.ts src/web/packages/editor/index.html
git commit -m "build: add editor shell UI foundation"
```

